import styled from 'styled-components';
import { ReactComponent as AlertSVG } from '@assets/alert.svg';
import { Icon } from '../Icon';

export const Alert = styled(Icon).attrs({ icon: AlertSVG })``;
