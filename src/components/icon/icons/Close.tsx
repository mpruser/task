import styled from 'styled-components';
import { ReactComponent as CloseSVG } from '@assets/close.svg';
import { Icon } from '../Icon';

export const Close = styled(Icon).attrs({ icon: CloseSVG })``;
