import styled from 'styled-components';
import { ReactComponent as SearchSVG } from '@assets/search.svg';
import { Icon } from '../Icon';

export const Search = styled(Icon).attrs({ icon: SearchSVG })``;
