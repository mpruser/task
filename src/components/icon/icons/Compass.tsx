import styled from 'styled-components';
import { ReactComponent as CompassSVG } from '@assets/compass.svg';
import { Icon } from '../Icon';

export const Compass = styled(Icon).attrs({ icon: CompassSVG })``;
